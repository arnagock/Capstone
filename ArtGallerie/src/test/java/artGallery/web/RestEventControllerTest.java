package artGallery.web;

import artGallery.AbstractArtGalleryIntegrationTests;





//@Sql("/test-data.sql")
	public class RestEventControllerTest extends AbstractArtGalleryIntegrationTests {
/*
		@Autowired
		MockMvc mockMvc;

		@Autowired
		EventRepository repo;
		
		@Test
		public void JoinEvent() throws Exception {
			mockMvc.perform(post("/events/111/toggle").contentType(APPLICATION_JSON)//
					.content("1"))//
					.andExpect(status().is2xxSuccessful());
			
			mockMvc.perform(post("/events/111/toggle").contentType(APPLICATION_JSON)//
					.content("1"))//
					.andExpect(status().is2xxSuccessful());}
	*/
		
	
	
		


	}